spring:
  application:
    name: service-accounting-statement
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        enabled: true
        username: accounting-svc
        password: Accounting$Cfg@1016#Prod
        server-addr: ${LIQUIDO_DISCOVER}
        namespace: ${spring.profiles.active}
      config:
        file-extension: yaml
        group: DEFAULT_GROUP
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        namespace: ${spring.profiles.active}
        username: accounting-svc
        password: Accounting$Cfg@1016#Prod
    sentinel:
      transport:
        dashboard: ${LIQUIDO_SENTINEL}