---
apiVersion: v1
kind: Service
metadata:
  namespace: NAMESPACE
  name: service-accounting-service-base
spec:
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
  type: NodePort
  selector:
    app.kubernetes.io/name: app-accounting-service-base
